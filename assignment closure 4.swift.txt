import Foundation

let oddSum = {
      (n :Int) //There shoudl be space after colon and not before. Why is there an extra tab space in the beginning?
    in
    
    var sum = 0
    for item in 1...n {
        if (item & 1 != 0) {
  
            sum += item
            print(sum) //Don't print the output in the core logic. Build an array with the result and return the array. The receiver can print it.
        }
    }
}
    print(oddSum(5))
    
    
